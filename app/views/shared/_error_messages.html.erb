<% if @project.errors.any? %>
	<p>This form contains <%= pluralize(@project.errors.count, "error") %>.</p.
	<ul>
		<% @project.errors.full_messages.each do |msg| %>
		<li> * <%= msg %>
		<% end %>
	</ul>
<% end %>