<% provide(:title, "Visual Effects") %>
<% if !@effects.empty? %>
	<% @effects.each do |project| %>
	<%= render 'project/project', :project => project %>
	<% end %>
<% else %>
	<div id="general_body">
		<h2>There are no projects in this catergory at this time.</h2>
	</div>
<% end %>