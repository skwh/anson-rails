<% provide(:title, "Editing #{@project.title}") %>
<div id="general_body">
	<h2 class="title">Edititng <%= @project.title %></h2>
	<div class="content">
		<%= form_for(@project) do |f| %>
			<p><%= f.label :title %><%= f.text_field :title %></p>
			<p><%= f.label :subtitle %><%= f.text_field :subtitle %></p>
			<p><%= f.label :description %><br />
			<%= f.text_area :description, size: "75x12" %></p>
			<p>Images:  <input type="button" value="New Image" id="new_image" class="<%= @project.id %>" /></p>
			<ol>
				<% @project.images.each do |image| %>
					<%= f.fields_for :images, image do |i| %>
						<li>Image large url:<%= i.text_field :large %>, Image thumb url:<%= i.text_field :thumbnail %>, <input type="button" value="Delete Image" class="delete_image_<%= image.id %>" /></li>
					<% end %>
				<% end %>
			</ol>
			<p>For videos, place the YouTube id of the video in a single string without spaces, separated by commas.</p>
			<p><%= f.label :videos %><%= f.text_field :videos %></p>
			<p><%= f.label :section %>
			<select id="project_section" name="project[section]" type="text">
				<option <% if @project.section=="design" %>selected<% end %> value="design">Animation</option>
				<option <% if @project.section=="art" %>selected<% end %> value="art">Digital Art</option>
				<option <% if @project.section=="effects" %>selected<% end %> value="effects">Visual Effects</option>
			</select></p>
			<%= f.submit "Submit" %>
		<% end %>
	</div>
</div>